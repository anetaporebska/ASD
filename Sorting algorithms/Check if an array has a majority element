def isMajority(arr):

    count={}
    for i in arr:
        if i in count:
            count[i]+=1
        else:
            count[i]=1
    print(count)
    for i in count:
        if count[i]>=len(arr)//2:
            return True


    return False
    
    
    
